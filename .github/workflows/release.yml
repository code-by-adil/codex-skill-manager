name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-release:
    name: Build macOS app archive
    runs-on: macos-14

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Show Swift version
        run: swift --version

      - name: Test
        run: swift test

      - name: Package app archive
        run: VERSION="${GITHUB_REF_NAME#v}" ./script/package_release.sh

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/release/*.zip
            dist/release/*.sha256
          body: |
            macOS build of Codex Skill Manager.

            Recommended install path:

            ```bash
            git clone ${{ github.server_url }}/${{ github.repository }}.git
            cd codex-skill-manager
            ./script/install.sh
            ```

            The zip asset is provided for convenience. macOS may require right-clicking the app and choosing Open the first time.
